# Coverage Map

> **Answers:** "Where are the gaps in our research?"
> **Shows:** topics from `config.yml.topics` and how many research items each one has
> **Rebuilt:** daily by the librarian

---

## Configured topics and current counts

(Populated by the librarian after the first scheduled run. Table will list each topic from `config.yml.topics` with file count and gap status.)

**Gap definition:** topic has < 3 research files. All topics are gaps initially.

---

## Suggested topic adjustments

(Librarian will populate this as research clusters around concepts not in `config.yml.topics`.)
